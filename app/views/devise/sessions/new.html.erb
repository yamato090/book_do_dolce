<main>
  <div class="container">
    <div class="row">
      <div class="col-md-12 offset-md-4">
        <h2>会員の方はこちらからログイン</h2>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

          <div class="form-group">
            <%= f.label :ニックネーム %></label>&emsp;&emsp;&emsp;
            <%= f.text_field :nickname, autofocus: true, autocomplete: "name" %>
          </div>

          <div class="form-group">
             <%= f.label :パスワード %>&emsp;&emsp;&emsp;&emsp;
            <%= f.password_field :password, autocomplete: "current-password" %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="form-group">
              <%= f.check_box :remember_me %>
              <%= f.label :ログイン状態を保持する %>
            </div>
           <% end %>

          <div class="form-group">
            <%= f.submit "ログイン" %>
          </div>
        <% end %>
        <div>会員登録がお済みでない方は<%= render "devise/shared/links" %>から新規登録を行ってください</div>
      </div>
    </div>
  </div>
</main>