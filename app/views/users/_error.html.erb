<% if model.errors.any? %>
  <div class="error">
    <h3><%= model.errors.count %> 件のエラーが発生したため ユーザー は保存されませんでした。</h3>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li class="error-message"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>